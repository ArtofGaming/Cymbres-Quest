using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro; // for TMP text UI
using System.IO; //File IO for text file scripts

public class TextBoxControllerScript : MonoBehaviour
{
    #region "Vars"
    public TextMeshProUGUI TextMesh;
    public TextAsset TextFile;

    #endregion

    //Create a file IO that will grab the correct text script for items.

    //Connect and fix the text script to work with the text file and read it correctly

    //CharacterName "textstuff" will be the format to read in the files

}